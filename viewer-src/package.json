{
  "name": "iiifpreserve-viewer",
  "private": true,
  "version": "0.0.0",
  "description": "Build-only project: produces the vendored Mirador 4 + MAE UMD bundle that go:embed ships in the iiifpreserve binary. The binary itself needs no Node — this is a one-time vendoring step (see Makefile `viewer` target).",
  "type": "module",
  "scripts": {
    "build": "vite build --config vite-umd.config.js"
  },
  "dependencies": {
    "mirador": "file:../../mirador",
    "mirador-annotation-editor": "1.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@mui/material": "^7.0.0",
    "@mui/system": "^7.0.0",
    "@emotion/react": "^11.10.6",
    "@emotion/styled": "^11.10.6",
    "react-i18next": "^15.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0"
  }
}
